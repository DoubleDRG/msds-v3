name: msds-v3
run-name: ${{github.actor}} is deploy Backend Projects with Github Actions.
on:
  push:
    branches:
      - backend

jobs:
  Build_And_Deploy_To_EB:
    runs-on: ubuntu-latest
    steps:
      - name: Download Code From Github
        uses: actions/checkout@v4
      - name: Install JDK-17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
      - name: Give Executable Permission To ./gradlew
        run: chmod +x ./gradlew
      - name: Build with Gradle
        run: ./gradlew clean build

      - name: Get Current-Time
        uses: 1466587594/get-current-time@v2
        id: current-time
        with:
          format: YYYY-MM-DDTHH-mm-ss
          utcOffset: "+09:00"
      - name: Show Current-Time
        run: echo "CurrentTime=${{steps.current-time.outputs.formattedTime}}"

#      - name: Generate deploy.zip
#        run:
#          mkdir deploy
#          cp build/libs/*.jar deploy/application.jar
#          cp Procfile deploy/Procfile
#          cp -r .ebextensions deploy/.ebextensions
#          cd deploy
#          zip -r deploy.zip .
#
#      - name: Deploy To EB
#        uses: einaregilsson/beanstalk-deploy@v21
#        with:
#          aws_access_key: ${{secrets.AWS_ACCESS_KEY}}
#          aws_secret_key: ${{secrets.AWS_SECRET_KEY}}
#          application_name: msds-v3
#          environment_name: Msds-v3-env
#          version_label: msds-v3-${{steps.current-time.outputs.formattedTime}}
#          region: ap-northeast-2
#          deployment_package: deploy/deploy.zip




